# Paper split manifests

The manifests live in the separate DisinfoMM dataset release because they contain source claims, explanations, and media URLs. Copy or link the release's `paper_splits/english` and `paper_splits/multilingual` directories here before running the configs.

This placeholder intentionally keeps third-party content out of the GitHub code repository.
